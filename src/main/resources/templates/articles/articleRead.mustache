{{>layouts/header}}
<br/>
<div id="posts_list">
    <div class="col-md-12">
        <fo rm class="card">
            <div class="card-header d-flex justify-content-between">
                <label for="id">번호 : {{article.id}}</label>
                <input type="hidden" id="id" value="{{article.id}}"> {{! label 연결 }}
            </div>
            <div class="card-body">
                <label for="title">제목</label>
                <input type="text" class="form-control" id="title" value="{{article.title}}" readonly>
                <br/>
                <label for="content">내용</label>
                <textarea rows="5" class="form-control" id="content" readonly>{{article.content}}</textarea>
            </div>
        </form>
        <a class="btn btn-primary" href="/articles/{{article.id}}/edit">글 수정</a>
        <a class="btn btn-danger" href="/articles/{{article.id}}/delete">글 삭제</a>
        <a href="/articles/new">새 글 작성</a>
        <a href="/articles">전체 글 목록</a>


        {{>comment/form}}
    </div>
</div>
{{>layouts/footer}}