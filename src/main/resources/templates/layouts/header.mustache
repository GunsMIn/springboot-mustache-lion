<!doctype html>
<html lang="ko">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>SpringBoot bbs</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-Zenh87qX5JnK2Jl0vWa8Ck2rdkQ2Bzep5IDxbcnCeuOxjzrPF/et3URy9Bv1WTRi" crossorigin="anonymous">
</head>
<body class="container">
{{#alertMessage}}
    <script type="text/javascript">
        window.onload = function () {
            alert('{{alertMessage}}')
            window.location.href = '/articles/list';
        }
    </script>
{{/alertMessage}}
<nav class="navbar navbar-expand-lg bg-primary navbar-dark">

    <div class="container-fluid">
        <a class="navbar-brand" href="/articles"><h4>게시판</h4></a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link" href="/articles">리스트</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/articles/new">글작성</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/hospitals">병원 리스트</a>
                </li>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        게시판 선택
                    </a>
                    <ul class="dropdown-menu">
                        <li><hr class="dropdown-divider"></li>
                        <li><a class="dropdown-item" href="/articles/list">게시판</a></li>
                        <li><a class="dropdown-item" href="/articles/new">글 작성</a></li>
                        <li><hr class="dropdown-divider"></li>
                        <li><a class="dropdown-item" href="/hospitals/list">병원 리스트</a></li>
                    </ul>
                </li>
            </ul>

            <form class="d-flex" role="search" action="/hospitals">
                {{#hospitalName}}
                    <input class="form-control me-2" type="text" class="form-control" name="hospitalName" value="{{hospitalName}}" placeholder="병원명으로 검색" aria-label="Search">
                {{/hospitalName}}
                {{^hospitalName}}
                    <input class="form-control me-2" name="hospitalName" type="search" placeholder="병원명으로 검색" aria-label="Search">
                {{/hospitalName}}
                <button class="btn btn-info" type="submit" >Search</button>
                {{#keyword}}
                    <input class="form-control me-2" type="text" class="form-control" name="keyword" value="{{keyword}}" placeholder="지역명으로 검색" aria-label="Search">
                {{/keyword}}
                {{^keyword}}
                    <input class="form-control me-2" name="keyword" type="search" placeholder="지역명으로 검색" aria-label="Search">
                {{/keyword}}
                <button class="btn btn-info" type="submit" >Search</button>
            </form>
        </div>
    </div>
</nav>
<br/>
<div style="margin-left: 30px;">